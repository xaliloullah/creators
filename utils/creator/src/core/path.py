class Path:
    base = "/"
    app = "app"
    config = "config"
    database = "database"
    docs = "docs"
    lang = "lang"
    public = "public"
    resources = "resources"
    routes = "routes"
    storage = "storage"
    utils = "utils"
    env = ".env"
    requirements = "requirements.json"
    controllers = f"{app}/controllers"
    models = f"{app}/models"
    middlewares = f"{app}/middlewares"
    architecture = f"{docs}/ARCHITECTURE.md"
    assets = f"{public}/assets"
    views = f"{resources}/views" 
    backups = f"{storage}/backups"
    sessions = f"{storage}/sessions"
    versions = f"{storage}/versions"
    creator = f"{utils}/creator"
    venv = f"{utils}/creator.venv"
    src = f"{creator}/src"
    settings = f"{src}/{app}/configs/settings.json"
    cache = f"{src}/{app}/cache"

